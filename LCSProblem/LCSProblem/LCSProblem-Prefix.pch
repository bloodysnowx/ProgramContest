//
// Prefix header for all source files of the 'LCSProblem' target in the 'LCSProblem' project
//

#ifdef __OBJC__
    #import <Foundation/Foundation.h>
#else
    #include <iostream>

    #if !defined(MAX)
        #define MAX(A,B)	({ __typeof__(A) __a = (A); __typeof__(B) __b = (B); __a < __b ? __b : __a; })
    #endif

#endif
