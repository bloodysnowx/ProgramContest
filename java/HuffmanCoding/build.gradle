apply plugin: 'java'
apply plugin: 'application'
repositories { mavenCentral() }
dependencies { testCompile 'junit:junit:4.+' }
// jar { manifest.mainAttributes("Main-Class" : "Main") }
mainClassName = "Main"
run {
    if(project.hasProperty('args')) {
        args project.args.split('\\s+')
    }
}